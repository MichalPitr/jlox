fun makeCounter() {
  var i = 0;
  fun count() {
    i = i + 1;
    print i;
  }

  return count;
}

var counter = makeCounter();
var counterT = makeCounter();

counter(); // "1".
counter(); // "2".
counterT(); // "1"
counterT(); // "2"
